import { FilterIngredientsPipe } from './filter-ingredients.pipe';

describe('FilterIngredientsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIngredientsPipe();
    expect(pipe).toBeTruthy();
  });
});
